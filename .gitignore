# === Environment & Secrets ===
.env
.env.*
.env.local
.env.production
js/env.js
*.pem
*.key
*.cert

# === Per-deployment server config ===
# Written by GPUCONFIGURE.bat → gpu-configure.html admin UI.
# Each operator picks their own resource tier; host-specific.
server/resource-config.json

# === Claude Code ===
.claude/pollinations-user.json
.claude/settings.local.json
.claude/start.bat
# Nested duplicate that the Claude Code CLI occasionally writes when
# invoked from a stray working directory. Not load-bearing, just noise.
.claude/.claude/

# === Workflow docs — tracked for project documentation ===
# (Previously ignored as "generated at runtime" but these are real docs now)

# === Pollinations output ===
pollinations-output/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
venv/
.venv/
env/
.env/
*.egg-info/
*.egg
dist/
build/
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# === Node ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn/cache
.yarn/unplugged
.pnp.*
package-lock.json

# === Server Brain Data ===
server/brain-weights*.json
server/conversations.json
server/episodic-memory.db
server/episodic-memory.db-wal
server/episodic-memory.db-shm
server/node_modules/
server/package-lock.json

# === OS junk ===
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
*.swp
*.swo
*~

# === IDE ===
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.project
.settings/

# === Temp & cache ===
tmp/
temp/
*.tmp
*.temp
*.log
*.cache
.cache/
.parcel-cache/

# === Build output ===
dist/
build/
out/
.next/
.nuxt/
.output/
js/app.bundle.js
